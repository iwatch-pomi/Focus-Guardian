{
  "buildCommand": "npm --prefix frontend run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "npm --prefix frontend install",
  "functions": {
    "api/backend.py": {
      "runtime": "python3.12",
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/v1/:path*",
      "destination": "/api/backend"
    }
  ],
  "crons": [
    {
      "path": "/api/v1/snapshot/manual",
      "schedule": "0 9 * * *"
    }
  ]
}
